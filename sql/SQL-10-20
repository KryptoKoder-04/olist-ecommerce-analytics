
-- SELECT * FROM olist_orders_dataset

------------ ANSWER (10 - 20) ------------------

-- 1️⃣1️⃣ Count orders by order_status

-- SELECT 
--     order_status,
--     COUNT(order_id) AS total_orders
-- FROM olist_orders_dataset
-- GROUP BY order_status
-- ORDER BY total_orders DESC;


-- Q1️⃣2️⃣ Find number of orders per customer

-- SELECT 
--      COUNT(customer_id)
-- FROM olist_customers_dataset

-- SELECT 
--      customer_id, 
-- 	 COUNT(order_id) As total_per_customer_order
-- FROM olist_orders_dataset
-- GROUP BY customer_id
-- ORDER BY total_per_customer_order DESC;

-- Q1️⃣3️⃣ Find customers who placed more than 1 order.

-- SELECT 
--      customer_id, 
-- 	 COUNT(order_id)  As total_per_customer_order
-- FROM olist_orders_dataset
-- GROUP BY customer_id
-- HAVING COUNT(order_id) > 1
-- ORDER BY total_per_customer_order DESC
-- LIMIT 5;


-- Q1️⃣4️⃣ Find total payment value per order.

-- SELECT 
--      order_id,
--      SUM(payment_value) AS total_payment
-- FROM olist_order_payments_dataset
-- GROUP BY order_id
-- ORDER BY total_payment DESC
-- LIMIT 10;

-- Q1️⃣5️⃣ (A).  Find orders where total payment > 500.

-- SELECT 
--      order_id,
--      SUM(payment_value) AS total_payment
-- FROM olist_order_payments_dataset
-- GROUP BY order_id
-- HAVING SUM(payment_value) > 500
-- ORDER BY total_payment DESC
-- LIMIT 10;


-- Q1️⃣5️⃣ (B).  Find Total count orders where total payment > 500.

-- SELECT COUNT(*) AS orders_above_500
-- FROM (
--     SELECT order_id
--     FROM olist_order_payments_dataset
--     GROUP BY order_id
--     HAVING SUM(payment_value) > 500
-- ) AS sub;


-- Q1️⃣6️⃣ Find number of orders per payment type.

-- SELECT 
--     DISTINCT payment_type, 
-- 	COUNT(order_id) As type_total
-- FROM olist_order_payments_dataset
-- GROUP BY payment_type
-- ORDER BY type_total DESC


-- Q1️⃣7️⃣ Find average payment value per payment type.

-- SELECT 
--     payment_type, 
-- 	AVG(payment_value) As type_total
-- FROM olist_order_payments_dataset
-- GROUP BY payment_type
-- ORDER BY type_total DESC

-- Q1️⃣8️⃣ Find top 5 orders with highest total payment.


-- SELECT 
--     order_id,
--     SUM(payment_value) AS total_payment
-- FROM olist_order_payments_dataset
-- GROUP BY order_id
-- ORDER BY total_payment DESC
-- LIMIT 5;


-- Q1️⃣9️⃣ (A). Count number of customers per city.

-- SELECT 
--      customer_city, 
-- 	 COUNT(customer_id) AS total
-- FROM public.olist_customers_dataset
-- GROUP BY  customer_city
-- HAVING 	 COUNT(customer_id)  > 500
-- ORDER BY total DESC
-- LIMIT 5;



-- Q1️⃣9️⃣ (B). Count number of customers per city.


-- SELECT COUNT(*) 
-- FROM (
--     SELECT 
--      customer_city, 
-- 	 COUNT(customer_id) AS total
-- FROM public.olist_customers_dataset
-- GROUP BY  customer_city
-- HAVING 	 COUNT(customer_id)  > 500
-- ORDER BY total DESC

-- ) AS sb;



-- Q2️⃣0️⃣ Find cities having more than 100 customers

-- SELECT COUNT(*) 
-- FROM (
--     SELECT 
--      customer_city, 
-- 	 COUNT(customer_id) AS total
-- FROM public.olist_customers_dataset
-- GROUP BY  customer_city
-- HAVING 	 COUNT(customer_id)  > 10
-- ORDER BY total DESC

-- ) AS sb;
